# Путь к файлу команд относительно корня проекта
commands_file: "../bhl-server/data/commands.json"

# Порог расстояния Левенштейна (0 = только точные совпадения, 3 = допускает небольшие опечатки)
threshold: 3
